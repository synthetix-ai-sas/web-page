Continuamos trabajando en el sitio de Synthetix AI (Astro 7 + pnpm, dark-mode design system). Contexto de la sesión anterior, antes de moverlo a este repo de GitHub:

**Estado real del i18n (el CLAUDE.md tiene esto desactualizado):** el bilingüe ES/EN ya está completo y funcionando — `src/i18n/ui.ts` (diccionario completo), rutas `/en/**` espejo de cada página, `src/i18n/utils.ts` (`useTranslations`, `localizePath`, `switchLangPath`), selector de idioma en el Navbar. `pnpm build` compila las 14 páginas sin errores. El punto "Opcional: bilingüe EN/ES" en el CLAUDE.md ya no aplica — habría que actualizarlo.

**Dos bugs de CSS ya arreglados y confirmados** (Chrome renderiza una costura blanca cuando un `border` — incluso transparente — está en un descendiente de un elemento con `backdrop-filter: blur()`; se reemplazó por `box-shadow` en ambos casos):
- `src/components/Navbar.astro`: `.sx-nav` — `border-bottom` → `box-shadow: 0 1px 0 var(--sx-slate-800)`.
- `src/components/Button.astro`: `.sx-btn` — `border: 1px solid transparent` → `box-shadow: inset 0 0 0 1px transparent`; `.sx-btn--outline` ajustado igual.

**Spec y plan ya escritos y aprobados, pero sin ejecutar todavía:**
- `docs/superpowers/specs/2026-07-23-i18n-followups-design.md`
- `docs/superpowers/plans/2026-07-23-i18n-followups.md` — 5 tareas:
  1. Tags SEO cross-idioma (canonical/hreflang/og:locale) — requiere `site` en `astro.config.mjs` (placeholder `https://synthetix.ai`, sin dominio real definido aún).
  2. Módulo `src/i18n/detect.ts` + auto-redirect silencioso según idioma del navegador.
  3. Persistir elección explícita del selector ES/EN del Navbar en `localStorage`.
  4. Página 404 bilingüe (`src/pages/404.astro`, hosting: Vercel).
  5. Fix de breakpoints intermedios en dos grids de 3 elementos (`.hero__stats` en Home, `.grid-3` en Nosotros) que colapsaban 3→1 columnas sin paso intermedio, inconsistente con el resto del sitio.

**Por qué estamos migrando a git/GitHub ahora:** el plan se iba a ejecutar con la skill `superpowers:subagent-driven-development`, pero esa skill depende de git (commits por tarea, `git diff` para las revisiones, ledger de progreso, worktrees) y el proyecto no tenía repo. Decidimos crear el repo en GitHub a mano en vez de que yo lo hiciera con `gh` (no está instalado en esa máquina).

**Lo primero que hay que hacer en esta sesión nueva, antes de retomar el plan:**
1. Confirmar que el repo local está bien enlazado al remoto de GitHub (`git remote -v`, `git status`).
2. Crear un `.gitignore` correcto para este stack (se creó el repo en GitHub con plantilla "None", así que probablemente no existe uno todavía, o solo trae lo que se subió a mano) — como mínimo: `node_modules/`, `dist/`, `.astro/`, `.superpowers/` (carpeta de scratch de la skill de subagentes, se genera sola), y los típicos `.env`/`.DS_Store`.
3. Retomar la ejecución del plan de 5 tareas con `superpowers:subagent-driven-development` (ya no debería haber conflicto de git).

**Preferencia de comunicación:** responder siempre en español (preguntas, respuestas, resúmenes) — el código, comentarios y nombres de archivo se mantienen en inglés, siguiendo la convención ya establecida en el proyecto.
